{
  "scripts": {
    "bundle": "bundle install",
    "test": "bundle exec rspec",
    "test-watch": "bundle exec guard",
    "rubocop": "bundle exec rubocop",
    "build": "gem build bbservices.gemspec"
  }
}